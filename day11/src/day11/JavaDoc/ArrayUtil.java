package day11.JavaDoc;
/**
 * 该类是数组操作/算法的工具类，提供了大量的静态方法用于操作数组，比如排序，搜索，等等。
 * @author Frame
 */
public class ArrayUtil {
	//私有化其构造器
	private ArrayUtil(){
		
	}
	/**
	 * 使用xxx算法，对指定的数组进行排序
	 * @param arr 要排序的数组
	 */
	public static void ArraySort(int[] arr){
		
	}
	/**
	 * 	     使用二分搜索法来搜索指定的 int 型数组，以获得指定的值。必须在进行此调用之前对数组进行排序（通过 sort(int[]) 方法）。如果没有对数组进行排序，
	 * 则结果是不确定的。如果数组包含多个带有指定值的元素，则无法保证找到的是哪一个
	 * @param arr 目标数组
	 * @param key 需要搜索的数据
	 * @return 如果它包含在数组中，则返回搜索键的索引；否则返回 (-(插入点) - 1)。
	 */
	public static int binarySearch(int[] arr,int key){
		return -1;
	}
}
